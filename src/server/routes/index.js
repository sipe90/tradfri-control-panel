
const gateway = require('routes/gateway')
const lights = require('routes/lights')
const sensors = require('routes/sensors')
const groups = require('routes/groups')

module.exports = { gateway, lights, sensors, groups }
